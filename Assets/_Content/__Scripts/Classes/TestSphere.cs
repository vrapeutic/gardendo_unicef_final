using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestSphere : MonoBehaviour
{
   /*
    void Start()
    {
        if (Statistics.isCompleteCourse)
        {
            this.gameObject.SetActive(true);
        }
    }

    */
}
